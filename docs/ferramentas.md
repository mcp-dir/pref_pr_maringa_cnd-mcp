# Ferramentas

Prefeitura PR Maringá: Certidão Negativa de Débitos (Contribuinte) expõe 1 ferramenta (todas somente leitura).

### 1. `pref_pr_maringa_cnd_consultar`
**Input**: `cnpj` (opcional), `cpf` (opcional), `nome_requerente` (opcional), `cnpj_requerente` (opcional), `cpf_requerente` (opcional), `finalidade` (opcional)

Prefeitura PR Maringá: Certidão Negativa de Débitos (Contribuinte), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Prefeitura PR Maringá: Certidão Negativa de Débitos (Contribuinte)
```
